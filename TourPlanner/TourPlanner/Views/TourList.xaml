<UserControl x:Class="TourPlanner.Views.TourList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlanner.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListView BorderBrush="DimGray" Margin="10 10 5 5"
                  ItemsSource="{Binding Tours, Mode=TwoWay}">
            <ListViewItem>
                <TextBlock>Tours:</TextBlock>
            </ListViewItem>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <RadioButton Content="{Binding TourName}"></RadioButton>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
